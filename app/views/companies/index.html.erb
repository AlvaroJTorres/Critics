<section class="section--xs">
  <div class="container">
    <div class="companies">
      <h1 class="heading heading--sn">Companies</h1>

      <% if policy(Company).create? %>
        <%= link_to "New Company", new_company_path, class: "btn btn--primary btn--small fit-content" %>
      <% end %>

      <%= render partial: "card", collection: @companies, as: :company %>

    </div>
  </div>
</section>