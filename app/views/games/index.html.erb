<section class="section--xs">
  <div class="container">
    <div class="companies">
      <h1 class="heading heading--sn">Games</h1>

      <% if policy(Game).create? %>
        <%= link_to "New Game", new_game_path, class: "btn btn--primary btn--small fit-content" %>
      <% end %>

      <%= render partial: "card", collection: @games, as: :game %>

    </div>
  </div>
</section>